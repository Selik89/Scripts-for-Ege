import random

pairs = [
    (250, 10),    # 0.04
    (250, 25),    # 0.10
    (250, 50),    # 0.20
    (250, 5),     # 0.02
    (500, 20),    # 0.04
    (500, 25),    # 0.05
    (500, 50),    # 0.10
    (500, 100),   # 0.20
    (1000, 25),   # 0.025
    (1000, 50),   # 0.05
    (1000, 100),  # 0.10
    (1000, 200),  # 0.20
    (1000, 250),  # 0.25
    (2000, 40),   # 0.02
    (2000, 50),   # 0.025
    (2000, 100),  # 0.05
    (2000, 200),  # 0.10
    (2000, 400),  # 0.20
    (1600, 16),   # 0.01
    (1600, 32),   # 0.02
    (1600, 80),   # 0.05
    (1600, 160),  # 0.10
    (1600, 320),  # 0.20
    (800, 8),     # 0.01
    (800, 16),    # 0.02
    (800, 40),    # 0.05
    (800, 80),    # 0.10
    (800, 160),   # 0.20
    (400, 4),     # 0.01
    (400, 8),     # 0.02
    (400, 20),    # 0.05
    (400, 40),    # 0.10
    (400, 80),    # 0.20
    (1000, 125),  # 0.125
    (2000, 250),  # 0.125
    (1600, 200),  # 0.125
    (800, 100),   # 0.125
    (400, 50),    # 0.125
    (250, 25),    # 0.10
    (500, 125),   # 0.25
    (1000, 400),  # 0.40
    (2000, 800),  # 0.40
    (1600, 640),  # 0.40
    (800, 320),   # 0.40
    (400, 160),   # 0.40
]

total_participants, reserve_aud = random.choice(pairs)
main_aud_total = total_participants - reserve_aud

first_aud = main_aud_total // 2
second_aud = main_aud_total - first_aud

prob_reserve = reserve_aud / total_participants

text = f"""
На олимпиаде по русскому языку {total_participants} участников разместили в трёх аудиториях.
В первых двух удалось разместить по {first_aud} человек, оставшихся перевели в запасную аудиторию в другом корпусе.
Найдите вероятность того, что случайно выбранный участник писал олимпиаду в запасной аудитории.
"""

text_of_solve = f"""
Общее число участников: {total_participants}
В первой аудитории: {first_aud}
Во второй аудитории: {second_aud}
В запасной аудитории: {reserve_aud}

Вероятность того, что случайно выбранный участник писал олимпиаду в запасной аудитории:
P = {reserve_aud}/{total_participants} = {prob_reserve}

Ответ: {prob_reserve}
"""

print(text)
print(prob_reserve)
print('__________________________')
print(text_of_solve)