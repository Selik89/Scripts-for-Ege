в строке ровно одно число повторяется трижды, остальные числа без повторений
в строке ровно одно число повторяется дважды, остальные числа без повторений
в строке ровно одно число повторяется четырежды, остальные числа без повторений

в строке ровно два числа повторяется дважды, остальные числа без повторений
в строке ровно три числа повторяется дважды, остальные числа без повторений

в строке все числа различны


